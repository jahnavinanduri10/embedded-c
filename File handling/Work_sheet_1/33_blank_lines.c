#include <stdio.h>

/*
while(fgets(line,100,fp))
{
int blank = 1;
len = strlen(line);

for(int i = 0;i < len;i++)
{
if(!isspace(line[i]))
{
isblank = 0;
break;
}
}
if(isblank)
    blankcount++;
}
*/