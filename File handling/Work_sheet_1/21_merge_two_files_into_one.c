#include <stdio.h>

int main()
{
    /*
    FILE *new = fopen("new.txt","w");
    *fp1 = fopen("first.txt","r");
     *fp2 = fopen("second.txt","r");
    char buff[100];
    while(fgets(buff,100,fp1) != NULL)
    {
        fputs(buff,new);
    }

    fseek(fp,0,SEEK_END)
    while(fgets(buff,100,fp2) != NULL)
    {
        fputs(buff,new);
    }

    */
}